"""Zotero MCP tool modules.

Each submodule registers tools on the shared ``mcp`` instance from
``zotero_mcp.server``.  The modules are imported at the bottom of
``server.py`` after the ``mcp`` object has been created.
"""
